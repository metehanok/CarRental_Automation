@model IEnumerable<Arac_Kiralama_Otomasyonu.Models.Subeler>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.sube_ad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.adres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.telefon)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.sube_ad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.adres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.telefon)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.sube_no }) |
                @Html.ActionLink("Details", "Details", new { id=item.sube_no }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.sube_no })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
